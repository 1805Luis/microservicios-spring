server: 
    port: 8761
eureka:
    client:
        fech-registry: false
        register-with-eureka: false
        service-url:
            default-zone: http://localhost:8761/{eureka.instance.hostname}:${server.port}/eureka/

